<Window x:Class="Traffic_Laws.RulesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Traffic_Laws"
        mc:Ignorable="d"
        Title="Rules" Height="720" Width="1080" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.Background>
            <ImageBrush ImageSource="\images\mainImage.png"/>
        </Grid.Background>
        <Grid Width="1080" Height="100" VerticalAlignment="Top">
            <Button  Style="{DynamicResource ButtonStyleMainMenu}" Foreground="White" Margin="0,40,0,0" HorizontalAlignment="Center" VerticalAlignment="Top">
                <TextBlock>Правила</TextBlock>
            </Button>
            <Button Width="100" Height="60" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="20" FontSize="30" Content="Меню" Click="Menu_Button_Click" Style="{DynamicResource ButtonStyleMainMenu}" Foreground="White"/>
        </Grid>
    </Grid>
</Window>
